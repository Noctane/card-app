import React, { Component } from 'react';
import './CardButton.css';

class CardButton extends Component {

    render() {
        return (
            <a href="#" className="CardButton">Click me</a>
        );
    }
}

export default CardButton;